services:
  academico-gateway:
    build:
      context: .
    image: academico-gateway:local
    container_name: academico-gateway
    restart: always
    network_mode: host
    env_file:
      - .env
    environment:
      - NODE_ENV=production
