apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: academico-gateway
  namespace: academico-prod
  labels:
    app.kubernetes.io/name: academico-gateway
    app.kubernetes.io/part-of: academico
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: academico-gateway
